spring:
  application:
    name: video-service
  profiles:
    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
video:
  storage:
    path: /path/to/storage/
logging:
  level:
    root: debug