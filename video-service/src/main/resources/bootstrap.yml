spring:
  application:
    name: video-service
  cloud:
    config:
      uri: http://localhost:8888
      name: video-service
      label: main
      profile: dev
      enabled: true
  config:
    import: configserver:http://localhost:8888