spring:
        application:
                name: api-gateway
        cloud:
                config:
                        uri: http://localhost:8888
                        name: api-gateway
                        label: master
                        profile: dev
                        enabled: true
        config:
                import: configserver:http://localhost:8888