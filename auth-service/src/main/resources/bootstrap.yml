spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      name: auth-service
      label: main
      profile: dev
      enabled: true
  config:
    import: configserver:http://localhost:8888